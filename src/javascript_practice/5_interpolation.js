const username = 'Kenn';
console.log(`${username} is true JS Wizard`);

console.log(`Hey students,
what bugs can
${username}
for you today?`);

const person = {
    username: 'Kenn',
    profession: 'Secret Agent'
};

console.log(`${person.username}, ${person.profession}, extraordinare`);